version: '3.8'

services:
  homelab-dashboard:
    build: .
    container_name: homelab-dashboard
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./config.json:/app/config.json
